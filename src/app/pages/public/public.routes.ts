import { Routes } from '@angular/router';
import { UnauthorizedComponent } from './unauthorized.component';

export const PUBLIC_ROUTES: Routes = [{ path: 'unauthorized', component: UnauthorizedComponent }];
